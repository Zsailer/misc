from setuptools import setup

setup(name='misc',
      version='0.1',
      description='Miscellaneous python modules for things I found myself doing often',
      author='Zach Sailer',
      author_email='zachsailer@gmail.com',
      packages=['misc'],
      install_requires=[
          'numpy'
      ],
      zip_safe=False)
